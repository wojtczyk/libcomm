PROJECT(ROOT)

SET(PACKAGE comm)
SET(VERSION 0.1.0)

SET(PACKAGE_NAME ${PACKAGE})
SET(PACKAGE_VERSION ${VERSION})
SET(PACKAGE_BUGREPORT wojtczyk@in.tum.de)
SET(PACKAGE_STRING "${PACKAGE_NAME} ${PACKAGE_VERSION} - A communication library")
SET(PACKAGE_TARNAME ${PACKAGE})

SUBDIRS(src)
